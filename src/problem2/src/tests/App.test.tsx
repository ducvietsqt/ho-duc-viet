import {render, screen} from '@testing-library/react';

import App from '../App';

// test('renders learn react link', () => {
//   render(<App />);
//   const linkElement = screen.getByText(/Design by Ho Duc Viet/i);
//   expect(linkElement).toBeInTheDocument();
// });

test('Check to see if the button is disabled when there is no currency.', () => {});
